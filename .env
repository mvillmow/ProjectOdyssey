# Docker container user/group IDs
# Default: 1000/1000 (common on Linux)
# Customize if your host system uses different IDs to avoid permission issues
# Run 'id -u' and 'id -g' to check your host values
USER_ID=1000
GROUP_ID=1000
