name: Integration Tests

# Run integration tests for component interactions
# Target duration: < 8 minutes

on:
  # Run on all pull requests (except drafts)
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

  # Run on pushes to main
  push:
    branches:
      - main

  # Allow manual runs
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write  # For PR comments

jobs:
  # ============================================================================
  # Integration Tests - Matrix Strategy
  # ============================================================================
  test-integration:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    # Skip draft PRs
    if: github.event.pull_request.draft == false || github.event_name != 'pull_request'

    strategy:
      fail-fast: false  # Run all suites even if one fails
      matrix:
        test-suite:
          - mojo-integration
          - python-integration
          - shared-integration

    steps:
      - name: Checkout code
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # v5.0.1

      - name: Set up Pixi
        uses: prefix-dev/setup-pixi@v0.9.3
        with:
          pixi-version: latest
          cache: true

      - name: Cache Pixi environments
        uses: actions/cache@v4
        with:
          path: ~/.pixi
          key: pixi-${{ runner.os }}-${{ hashFiles('pixi.toml') }}
          restore-keys: |
            pixi-${{ runner.os }}-

      - name: Cache test data fixtures
        uses: actions/cache@v4
        with:
          path: tests/fixtures
          key: test-data-${{ hashFiles('tests/fixtures/**') }}
          restore-keys: |
            test-data-

      - name: Run integration test suite - ${{ matrix.test-suite }}
        run: |
          echo "Running integration tests for: ${{ matrix.test-suite }}"
          mkdir -p test-results

          # Map test suite to actual test paths
          case "${{ matrix.test-suite }}" in
            mojo-integration)
              echo "Running Mojo integration tests..."
              # Run tests/integration/*.mojo files
              if [ -d "tests/integration" ] && [ -n "$(find tests/integration -maxdepth 1 -name '*.mojo' -o -name '*.🔥' 2>/dev/null)" ]; then
                EXIT_CODE=0
                for file in tests/integration/*.mojo; do
                  if [ -f "$file" ]; then
                    echo "Running $file..."
                    pixi run mojo -I . "$file" || EXIT_CODE=$?
                  fi
                done
                exit $EXIT_CODE
              else
                echo "⚠️  No Mojo integration tests found in tests/integration/"
                echo "No Mojo integration tests found" > test-results/mojo-integration-results.txt
              fi
              ;;

            python-integration)
              echo "Running Python integration tests..."
              # Run Python integration tests (agents, foundation structure, etc.)
              TEST_FILES=""
              [ -f "tests/agents/test_integration.py" ] && TEST_FILES="$TEST_FILES tests/agents/test_integration.py"
              [ -f "tests/foundation/test_structure_integration.py" ] && TEST_FILES="$TEST_FILES tests/foundation/test_structure_integration.py"

              if [ -n "$TEST_FILES" ]; then
                pixi run python -m pytest $TEST_FILES --verbose --maxfail=3 --timeout=180
                echo "✅ Python integration tests completed"
              else
                echo "⚠️  No Python integration tests found"
                echo "No Python integration tests found" > test-results/python-integration-results.txt
              fi
              ;;

            shared-integration)
              echo "Running shared library integration tests..."
              # Run tests/shared/integration/*.mojo files
              if [ -d "tests/shared/integration" ] && [ -n "$(find tests/shared/integration -name '*.mojo' -o -name '*.🔥' 2>/dev/null)" ]; then
                EXIT_CODE=0
                for file in tests/shared/integration/*.mojo; do
                  if [ -f "$file" ]; then
                    echo "Running $file..."
                    pixi run mojo -I . "$file" || EXIT_CODE=$?
                  fi
                done
                exit $EXIT_CODE
              else
                echo "⚠️  No shared integration tests found in tests/shared/integration/"
                echo "No shared integration tests found" > test-results/shared-integration-results.txt
              fi
              ;;

            *)
              echo "❌ Unknown test suite: ${{ matrix.test-suite }}"
              exit 1
              ;;
          esac

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: integration-results-${{ matrix.test-suite }}
          path: test-results/
          retention-days: 7

      - name: Upload failure artifacts
        if: failure()
        uses: actions/upload-artifact@v5
        with:
          name: integration-failures-${{ matrix.test-suite }}
          path: |
            test-results/
            logs/
            *.log
          retention-days: 14

  # ============================================================================
  # Integration Test Report
  # ============================================================================
  integration-report:
    needs: test-integration
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: Checkout code
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # v5.0.1

      - name: Download all test results
        uses: actions/download-artifact@v6
        with:
          pattern: integration-results-*
          path: all-results/

      - name: Generate integration test report
        run: |
          echo "# 🔗 Integration Test Results" > integration-report.md
          echo "" >> integration-report.md
          echo "**Workflow Run**: [\`${{ github.run_number }}\`](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> integration-report.md
          echo "" >> integration-report.md

          # Count results
          TOTAL_SUITES=3
          PASSED_SUITES=0
          FAILED_SUITES=0

          # Check each test suite result
          for suite in mojo-integration python-integration shared-integration; do
            echo "## Test Suite: \`$suite\`" >> integration-report.md

            RESULT_DIR="all-results/integration-results-$suite"

            if [ -d "$RESULT_DIR" ]; then
              if grep -q "No.*integration tests found" "$RESULT_DIR"/*.txt 2>/dev/null; then
                echo "⚠️  No tests implemented yet" >> integration-report.md
                PASSED_SUITES=$((PASSED_SUITES + 1))
              else
                echo "✅ Tests passed" >> integration-report.md
                PASSED_SUITES=$((PASSED_SUITES + 1))
              fi
            else
              echo "❌ Tests failed or did not run" >> integration-report.md
              FAILED_SUITES=$((FAILED_SUITES + 1))
            fi

            echo "" >> integration-report.md
          done

          # Summary
          echo "## Summary" >> integration-report.md
          echo "" >> integration-report.md
          echo "- **Total Suites**: $TOTAL_SUITES" >> integration-report.md
          echo "- **Passed**: $PASSED_SUITES" >> integration-report.md
          echo "- **Failed**: $FAILED_SUITES" >> integration-report.md
          echo "" >> integration-report.md

          # Check for failures
          AGENT_RESULT="${{ needs.test-integration.result }}"
          if [ "$AGENT_RESULT" = "success" ]; then
            echo "✅ **All integration tests passed!**" >> integration-report.md
          else
            echo "❌ **Some integration tests failed. Please review the logs.**" >> integration-report.md
          fi

          # Display report
          cat integration-report.md

      - name: Upload integration report
        uses: actions/upload-artifact@v5
        with:
          name: integration-report
          path: integration-report.md
          retention-days: 30

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v8
        with:
          script: |
            const fs = require('fs');

            try {
              const report = fs.readFileSync('integration-report.md', 'utf8');

              // Check if a comment already exists
              const { data: comments } = await github.rest.issues.listComments({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
              });

              const botComment = comments.find(comment =>
                comment.user.type === 'Bot' &&
                comment.body.includes('🔗 Integration Test Results')
              );

              if (botComment) {
                // Update existing comment
                await github.rest.issues.updateComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  comment_id: botComment.id,
                  body: report
                });
                console.log('Updated existing PR comment');
              } else {
                // Create new comment
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.issue.number,
                  body: report
                });
                console.log('Created new PR comment');
              }
            } catch (error) {
              console.error('Failed to comment on PR:', error);
            }

      - name: Check test results
        run: |
          # Fail if any test suite failed
          if [ "${{ needs.test-integration.result }}" != "success" ]; then
            echo "❌ Integration tests failed"
            exit 1
          fi

          echo "✅ All integration tests passed"
