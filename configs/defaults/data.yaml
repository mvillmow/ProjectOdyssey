# Default Data Configuration
# Data loading and preprocessing defaults

data:
  dataset_root: "${ML_ODYSSEY_DATA:-./data}"
  num_workers: 4
  pin_memory: true
  persistent_workers: true

preprocessing:
  normalize: true
  augmentation: false

validation:
  split: 0.1
  stratified: true
