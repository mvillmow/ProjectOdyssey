# Default Path Configuration
# Centralized path management with environment variable support

paths:
  data_dir: "${ML_ODYSSEY_DATA:-./data}"
  checkpoint_dir: "${ML_ODYSSEY_CHECKPOINTS:-./checkpoints}"
  log_dir: "${ML_ODYSSEY_LOGS:-./logs}"
  output_dir: "${ML_ODYSSEY_OUTPUT:-./output}"
  cache_dir: "${ML_ODYSSEY_CACHE:-./cache}"
