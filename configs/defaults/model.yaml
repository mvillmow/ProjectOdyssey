# Default Model Configuration
# Generic model settings that apply to all architectures

model:
  dtype: "float32"
  init_method: "xavier"
  dropout: 0.0
  batch_norm: false

loss:
  name: "cross_entropy"
  label_smoothing: 0.0

metrics:
  - "accuracy"
  - "loss"
