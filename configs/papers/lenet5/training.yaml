# LeNet-5 Training Configuration
# Based on original paper specifications

extends: "defaults/training.yaml"

optimizer:
  name: "sgd"
  learning_rate: 0.001
  momentum: 0.9

training:
  epochs: 20
  batch_size: 64

lr_scheduler:
  enabled: true
  type: "step"
  step_size: 10
  gamma: 0.1
