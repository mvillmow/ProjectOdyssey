# Pre-commit hooks for ML Odyssey
# See https://pre-commit.com for more information

repos:
  # Mojo formatting (DISABLED - bug in mojo format removes space in "inout self")
  # See: https://github.com/modular/modular/issues/5573
  # TODO: Re-enable when bug is fixed
  # - repo: local
  #   hooks:
  #     - id: mojo-format
  #       name: Mojo Format
  #       description: Format Mojo source files using mojo format (skips if mojo not available)
  #       entry: bash -c 'if command -v mojo &> /dev/null; then mojo format "$@"; else echo "⚠️  Mojo not found, skipping format"; exit 0; fi' --
  #       language: system
  #       files: \.(mojo|🔥)$
  #       pass_filenames: true

  # Security checks
  - repo: local
    hooks:
      - id: check-shell-injection
        name: Check for shell=True (Security)
        description: Prevent command injection vulnerabilities via shell=True
        entry: 'shell=True'
        language: pygrep
        files: \.py$
        types: [python]

  # Markdown linting
  - repo: https://github.com/DavidAnson/markdownlint-cli2
    rev: v0.12.1
    hooks:
      - id: markdownlint-cli2
        name: Markdown Lint
        description: Lint markdown files for consistency
        exclude: ^notes/(plan|issues|review|blog)/
        args: ['--config', '.markdownlint.json']

  # General file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
        name: Trim Trailing Whitespace
        exclude: ^notes/(plan|review|issues)/
      - id: end-of-file-fixer
        name: Fix End of Files
        exclude: ^notes/(plan|review|issues)/
      - id: check-yaml
        name: Check YAML
      - id: check-added-large-files
        name: Check for Large Files
        args: ['--maxkb=1000']
      - id: mixed-line-ending
        name: Fix Mixed Line Endings
        exclude: ^notes/(plan|review|issues)/
