# Valid training configuration for testing
optimizer:
  name: sgd
  learning_rate: 0.01
  momentum: 0.9

scheduler:
  name: step
  step_size: 10
  gamma: 0.1

training:
  epochs: 100
  batch_size: 64
  validation_split: 0.2
